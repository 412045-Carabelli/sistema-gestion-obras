*** Begin Patch
*** Update File: backend1.0/obras-service/src/main/java/com/obras/controller/EstadosObrasController.java
@@
-package com.obras.controller;
-
-import com.obras.enums.EstadoObraEnum;
-import org.springframework.http.ResponseEntity;
-import org.springframework.web.bind.annotation.GetMapping;
-import org.springframework.web.bind.annotation.RequestMapping;
-import org.springframework.web.bind.annotation.RestController;
-
-import java.util.Arrays;
-import java.util.List;
-import java.util.stream.Collectors;
-
-@RestController
-@RequestMapping("/api/obras/estados")
-public class EstadosObrasController {
-
-    @GetMapping
-    public ResponseEntity<List<String>> getEstados() {
-        List<String> estados = Arrays.stream(EstadoObraEnum.values())
-                .map(Enum::name)
-                .collect(Collectors.toList());
-        return ResponseEntity.ok(estados);
-    }
-}
+package com.obras.controller;
+
+import com.obras.enums.EstadoObraEnum;
+import org.springframework.http.ResponseEntity;
+import org.springframework.web.bind.annotation.GetMapping;
+import org.springframework.web.bind.annotation.RequestMapping;
+import org.springframework.web.bind.annotation.RestController;
+import org.slf4j.Logger;
+import org.slf4j.LoggerFactory;
+
+import java.util.Arrays;
+import java.util.List;
+import java.util.stream.Collectors;
+
+@RestController
+@RequestMapping("/api/obras/estados")
+public class EstadosObrasController {
+    private static final Logger log = LoggerFactory.getLogger(EstadosObrasController.class);
+
+    @GetMapping
+    public ResponseEntity<List<String>> getEstados() {
+        List<String> estados = Arrays.stream(EstadoObraEnum.values())
+                .map(Enum::name)
+                .collect(Collectors.toList());
+        log.info("GET /api/obras/estados -> {}", estados);
+        return ResponseEntity.ok(estados);
+    }
+}
*** End Patch
